# @company/shared-graphql

Shared business logic, types, and helpers for Payee validation used by mobile and web BFFs.

Quick start (from package root):

Install dependencies:

```bash
cd shared-graphql
npm install
```

Run the example (uses `ts-node`):

```bash
npm run start:example
```

Build the package:

```bash
npm run build
```
